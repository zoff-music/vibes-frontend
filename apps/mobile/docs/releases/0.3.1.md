# 0.3.1

- Fix Android tab switching so the selected tab matches the displayed screen.
- Keep chat messages compact and anchored above the composer.
- Keep the chat input visible when the keyboard opens.
- Virtualize mobile chat and retain at most 500 messages per room.
- Restore shared scrolling for Now Playing and the mobile queue.
- Show a compact Live rooms preview, with search and pagination in Browse.
- Remove the queue tab selection styling when chat is disabled.

Native build numbers are managed by EAS.
